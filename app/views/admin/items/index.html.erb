<div class="page-header">
  <h1>ІНВЕНТАР ТОВАРІВ 🎸</h1>
  <div class="page-actions">
    <%= link_to "ДОДАТИ НОВИЙ ТОВАР +", new_admin_item_path, class: "btn-primary" %>
  </div>
</div>

<div class="item-card item-card--flush item-card--dark">
  <table class="table--full">
    <thead>
      <tr>
        <th>Товар</th>
        <th>Ціна</th>
        <th>Кількість</th>
        <th>Статус</th>
        <th style="text-align: center;">Дії</th>
      </tr>
    </thead>
    <tbody>
      <% @items.each do |item| %>
        <tr class="table-row">
          <td>
            <div style="display: flex; align-items: center; gap: 10px;">
              <span style="font-size: 1.2rem;">🎸</span>
              <div>
                <div style="font-weight: bold;"><%= item.name %></div>
                <small class="muted"><%= item.id.to_s.first(8) %></small>
              </div>
            </div>
          </td>
          <td><%= number_to_currency(item.price, unit: "₴") %></td>
          <td>
            <span class="<%= 'low-stock' if item.quantity < 3 %>">
              <%= item.quantity %> шт.
            </span>
          </td>
          <td>
            <% if item.quantity > 0 %>
              <span class="status-available">● В продажу</span>
            <% else %>
              <span class="status-unavailable">● Немає</span>
            <% end %>
          </td>
          <td style="text-align: center;">
            <div style="display: flex; gap: 10px; justify-content: center; align-items: center;">

              <%= link_to "✏️", edit_admin_item_path(item), title: "Редагувати", class: "", style: "font-size: 1.2rem; text-decoration: none;" %>

              <%= button_to admin_item_path(item), 
                  method: :delete, 
                  form: { data: { turbo_confirm: "Ви впевнені? Це видалить товар назавжди!" } }, 
                  style: "background: none; border: none; cursor: pointer; color: #ff4500; font-size: 1.2rem;",
                  title: "Видалити" do %>
                🗑️
              <% end %>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>