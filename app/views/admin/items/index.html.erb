<div class="max-w-7xl mx-auto px-4 py-8">
  <%# Хедер залишаємо як був %>
  <div class="flex justify-between items-center mb-8 border-b border-stone-800 pb-6">
    <h1 class="text-3xl font-black text-white uppercase">ІНВЕНТАР 🎸</h1>
    <%= link_to "ДОДАТИ ТОВАР +", new_admin_item_path, class: "bg-primary text-white px-6 py-3 rounded-xl font-bold hover:brightness-110" %>
  </div>

  <%# Таблиця без приховування для тестів %>
  <div class="bg-stone-900 rounded-2xl border border-stone-800 overflow-x-auto">
    <table class="w-full text-left border-collapse">
      <thead>
        <tr class="text-stone-500 text-[10px] uppercase tracking-widest bg-stone-800/50">
          <th class="p-4">Товар</th>
          <th class="p-4">Ціна</th>
          <th class="p-4 text-center">Дії</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-stone-800">
        <% if @items.any? %>
          <% @items.each do |item| %>
            <tr class="hover:bg-stone-800/30 transition-colors">
              <td class="p-4 font-bold"><%= item.name %></td>
              <td class="p-4 text-primary font-mono"><%= number_to_currency(item.price, unit: "₴") %></td>
              <td class="p-4">
                <div class="flex justify-center gap-4">
                  <%= link_to "✏️", edit_admin_item_path(item) %>
                  <%= button_to "🗑️", admin_item_path(item), method: :delete, data: { turbo_confirm: "Видалити?" } %>
                </div>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td colspan="3" class="p-20 text-center text-stone-500 italic">
              Список товарів порожній (Перевір змінну @items у контролері)
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>