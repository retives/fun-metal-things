<div class="content-wrapper max-w-5xl mx-auto px-4">
  <div class="profile-header mb-8 border-b-2 border-primary pb-4">
    <h1 class="text-3xl font-black uppercase tracking-tight">Ваш кошик 🛒</h1>
  </div>

  <% if @cart_items.any? %>
    <div class="bg-stone-900 rounded-xl border border-dark-accent overflow-hidden shadow-2xl">
      
      <%# ДЕСКТОПНА ТАБЛИЦЯ (прихована на мобільних: hidden md:table) %>
      <table class="hidden md:table w-full border-collapse text-left">
        <thead>
          <tr class="bg-stone-800 text-stone-400 uppercase text-xs tracking-widest">
            <th class="p-4">Товар</th>
            <th class="p-4">Ціна</th>
            <th class="p-4">Кількість</th>
            <th class="p-4">Сума</th>
            <th class="p-4 text-right">Дії</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-stone-800">
          <% @cart_items.each do |cart_item| %>
            <tr class="hover:bg-stone-800/30 transition-colors">
              <td class="p-4 flex items-center gap-4">
                <div class="w-12 h-12 bg-stone-950 rounded border border-stone-800 flex items-center justify-center shrink-0">🎸</div>
                <strong class="text-white"><%= cart_item.item.name %></strong>
              </td>
              <td class="p-4 text-stone-300 font-mono"><%= number_to_currency(cart_item.item.price, unit: "₴") %></td>
              <td class="p-4">
                <%= render 'carts/quantity_controls', cart_item: cart_item %>
              </td>
              <td class="p-4 text-primary font-bold font-mono">
                <%= number_to_currency(cart_item.quantity * cart_item.item.price, unit: "₴") %>
              </td>
              <td class="p-4 text-right">
                <%= button_to cart_item_path(cart_item), 
                              method: :delete, 
                              data: { turbo_confirm: "Видалити цей товар?" },
                              class: "text-[10px] font-bold text-red-500 hover:text-red-400 border border-red-500/20 hover:border-red-500/50 px-3 py-1 rounded transition-all uppercase tracking-tighter" do %>
                  Видалити
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <%# МОБІЛЬНИЙ СПИСОК (показується тільки на мобільних: md:hidden) %>
      <div class="md:hidden divide-y divide-stone-800">
        <% @cart_items.each do |cart_item| %>
          <div class="p-4 flex flex-col gap-4">
            <div class="flex gap-4">
              <div class="w-16 h-16 bg-stone-950 rounded flex items-center justify-center text-2xl border border-stone-800">🎸</div>
              <div class="flex-1">
                <h3 class="text-white font-bold leading-tight"><%= cart_item.item.name %></h3>
                <p class="text-stone-500 text-sm mt-1"><%= number_to_currency(cart_item.item.price, unit: "₴") %></p>
              </div>
              <%= button_to "✕", cart_item_path(cart_item), method: :delete, class: "text-red-500 font-bold px-2" %>
            </div>
            
            <div class="flex items-center justify-between bg-stone-950/50 p-3 rounded-lg border border-stone-800">
              <%= render 'carts/quantity_controls', cart_item: cart_item %>
              <span class="text-primary font-bold font-mono">
                <%= number_to_currency(cart_item.quantity * cart_item.item.price, unit: "₴") %>
              </span>
            </div>
          </div>
        <% end %>
      </div>

      <div class="p-6 md:p-8 bg-stone-950 border-t border-stone-800">
        <div class="flex flex-col md:flex-row justify-between items-center gap-6">
          <div class="text-center md:text-left">
            <p class="text-stone-500 text-sm uppercase tracking-widest mb-1">Загальна вартість</p>
            <h2 class="text-3xl md:text-4xl font-black text-white">
              <span class="text-primary"><%= number_to_currency(@total_price, unit: "₴") %></span>
            </h2>
          </div>
          <div class="flex flex-col sm:flex-row gap-4 w-full md:w-auto">
            <%= link_to "До каталогу", items_path, class: "text-center text-stone-400 hover:text-white font-bold uppercase text-xs px-6 py-4 transition-colors" %>
            <%= link_to "Оформити замовлення", new_order_path, class: "btn-primary w-full sm:w-auto text-center !rounded-xl !py-4" %>
          </div>
        </div>
      </div>
    </div>
  <% else %>
    <div class="text-center py-24 bg-stone-900 rounded-3xl border border-dashed border-stone-800">
      <h2 class="text-stone-500 text-xl font-medium mb-6 uppercase tracking-widest">Ваш кошик порожній... поки що.</h2>
      <%= link_to "Перейти до каталогу", items_path, class: "btn-primary !inline-flex" %>
    </div>
  <% end %>
</div>