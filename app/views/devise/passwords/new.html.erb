<div class="min-h-[80vh] flex items-center justify-center px-4 py-12">
  <div class="w-full max-w-md bg-stone-900 rounded-3xl border border-stone-800 shadow-2xl overflow-hidden relative">
    
    <%# Декоративний акцент зверху (інженерний сірий для відновлення) %>
    <div class="h-2 w-full bg-stone-700 shadow-[0_0_20px_rgba(120,113,108,0.3)]"></div>

    <div class="p-8 md:p-10">
      <header class="text-center mb-8">
        <h2 class="text-2xl md:text-3xl font-black text-white uppercase tracking-tighter mb-4 leading-tight">
          ЗАБУЛИ ПАРОЛЬ? <span class="text-stone-500">🛠️</span>
        </h2>
        <p class="text-stone-500 text-xs md:text-sm leading-relaxed max-w-[280px] mx-auto">
          Введіть ваш email, і ми надішлемо інструкції для відновлення доступу.
        </p>
      </header>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "space-y-6" }) do |f| %>
        
        <%# Вивід помилок Devise %>
        <% if resource.errors.any? %>
          <div class="bg-red-900/20 border-l-4 border-red-500 p-3 rounded-r-lg mb-4">
            <ul class="text-[11px] text-red-400 font-bold uppercase space-y-1">
              <% resource.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="space-y-2">
          <%= f.label :email, "Електронна пошта", class: "admin-form-label" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", 
              class: "admin-form-input text-center", 
              placeholder: "metalhead@example.com" %>
        </div>

        <div class="pt-2">
          <%= f.submit "Надіслати інструкції", class: "btn-primary w-full !py-4 text-base font-black tracking-widest shadow-lg shadow-primary/20 cursor-pointer active:scale-95 transition-transform uppercase" %>
        </div>
      <% end %>

      <div class="mt-8 pt-6 border-t border-stone-800 text-center">
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>