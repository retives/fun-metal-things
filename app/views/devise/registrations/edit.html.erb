<div class="max-w-xl mx-auto px-4 py-12">
  <%# ЗАГОЛОВОК СТОРІНКИ %>
  <div class="mb-8 border-b-2 border-primary pb-4 flex items-center justify-between">
    <h1 class="text-2xl md:text-3xl font-black text-white uppercase tracking-tighter">
      Налаштування входу <span class="text-primary">🛡️</span>
    </h1>
    <%= link_to "← Назад", profile_path, class: "text-[10px] font-bold text-stone-500 hover:text-white uppercase tracking-widest transition-colors" %>
  </div>

  <div class="bg-stone-900 rounded-3xl border border-stone-800 shadow-2xl overflow-hidden">
    <div class="p-6 md:p-10">
      
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "space-y-6" }) do |f| %>
        
        <%# ПОМИЛКИ %>
        <% if resource.errors.any? %>
          <div class="bg-red-900/20 border-l-4 border-red-500 p-4 rounded-r-xl">
            <ul class="text-xs text-red-400 font-bold uppercase space-y-1">
              <% resource.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <%# EMAIL %>
        <div class="space-y-2">
          <%= f.label :email, "Електронна пошта", class: "admin-form-label" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "admin-form-input" %>
          
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <p class="text-[10px] text-amber-500 font-bold uppercase animate-pulse">
              Очікується підтвердження для: <%= resource.unconfirmed_email %>
            </p>
          <% end %>
        </div>

        <%# ПАРОЛІ %>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 pt-4 border-t border-stone-800">
          <div class="space-y-2">
            <%= f.label :password, class: "admin-form-label" do %>
              Новий пароль <span class="text-[9px] text-stone-600 font-normal normal-case block italic">(залиште порожнім, якщо не змінюєте)</span>
            <% end %>
            <%= f.password_field :password, autocomplete: "new-password", class: "admin-form-input", placeholder: "••••••••" %>
          </div>

          <div class="space-y-2">
            <%= f.label :password_confirmation, "Підтвердження", class: "admin-form-label" %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "admin-form-input", placeholder: "••••••••" %>
          </div>
        </div>

        <%# ПІДТВЕРДЖЕННЯ ПОТОЧНИМ ПАРОЛЕМ %>
        <div class="bg-stone-950 p-6 rounded-2xl border-2 border-primary/30 mt-8 space-y-3">
          <div class="flex items-center gap-3">
            <span class="text-xl">🔑</span>
            <%= f.label :current_password, class: "admin-form-label !mb-0" do %>
              Поточний пароль <span class="text-[9px] text-primary/70 block normal-case font-normal italic">необхідний для збереження будь-яких змін</span>
            <% end %>
          </div>
          <%= f.password_field :current_password, autocomplete: "current-password", 
              class: "admin-form-input !border-primary/50 focus:!border-primary shadow-[0_0_15px_rgba(207,109,55,0.1)]", 
              placeholder: "Введіть пароль для підтвердження" %>
        </div>

        <div class="pt-4">
          <%= f.submit "Оновити акаунт 🤘", class: "btn-primary w-full !py-4 text-base font-black tracking-widest shadow-xl cursor-pointer active:scale-95 transition-transform" %>
        </div>
      <% end %>

    </div>
  </div>

  <%# НЕБЕЗПЕЧНА ЗОНА %>
  <div class="mt-12 p-8 border-2 border-dashed border-red-900/50 rounded-3xl bg-red-900/5 text-center space-y-4">
    <h3 class="text-red-500 font-black uppercase tracking-widest text-sm">Видалити акаунт</h3>
    <p class="text-stone-500 text-xs leading-relaxed max-w-xs mx-auto">
      Це дія незворотна. Всі ваші замовлення будуть анонімізовані, а дані видалені.
    </p>
    <%= button_to "Видалити мій акаунт", registration_path(resource_name), 
        data: { turbo_confirm: "Ви впевнені? Це неможливо буде скасувати!" }, 
        method: :delete, 
        class: "text-red-500 hover:text-red-400 font-black text-[10px] uppercase tracking-widest border-b border-red-900/50 pb-1 hover:border-red-400 transition-all" %>
  </div>
</div>