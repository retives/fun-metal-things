<div class="content-wrapper" style="max-width: 450px; margin: 40px auto;">
  <div class="item-card" style="padding: 40px; border-top: 4px solid var(--primary);">
    <h2 style="text-align: center; margin-bottom: 30px;">СТАТИ ЧАСТИНОЮ FUNMETAL 🎸</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group" style="margin-bottom: 20px;">
        <%= f.label :email, "Email", style: "display: block; color: var(--primary); margin-bottom: 8px;" %>
        <%= f.email_field :email, autofocus: true, class: "form-control", 
            style: "width: 100%; background: #2b2724; border: 1px solid var(--muted); color: white; padding: 12px; border-radius: 6px;" %>
      </div>

      <div class="form-group" style="margin-bottom: 20px;">
        <%= f.label :password, "Пароль", style: "display: block; color: var(--primary); margin-bottom: 8px;" %>
        <%= f.password_field :password, class: "form-control", style: "width: 100%; background: #2b2724; border: 1px solid var(--muted); color: white; padding: 12px; border-radius: 6px;" %>
      </div>

      <div class="form-group" style="margin-bottom: 30px;">
        <%= f.label :password_confirmation, "Підтвердіть пароль", style: "display: block; color: var(--primary); margin-bottom: 8px;" %>
        <%= f.password_field :password_confirmation, class: "form-control", style: "width: 100%; background: #2b2724; border: 1px solid var(--muted); color: white; padding: 12px; border-radius: 6px;" %>
      </div>

      <%= f.submit "ЗАРЕЄСТРУВАТИСЯ", class: "btn-primary", style: "width: 100%;" %>
    <% end %>

    <div style="margin-top: 20px; text-align: center;">
      <%= link_to "Вже є акаунт? Увійти", new_session_path(resource_name), style: "color: var(--muted); text-decoration: none; font-size: 0.9rem;" %>
    </div>
  </div>
</div>