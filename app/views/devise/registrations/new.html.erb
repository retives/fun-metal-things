<div class="min-h-[85vh] flex items-center justify-center px-4 py-12">
  <div class="w-full max-w-md bg-stone-900 rounded-3xl border border-stone-800 shadow-2xl overflow-hidden relative">
    
    <%# Декоративний акцент зверху %>
    <div class="h-2 w-full bg-primary shadow-[0_0_20px_rgba(207,109,55,0.5)]"></div>

    <div class="p-8 md:p-10">
      <header class="text-center mb-8">
        <h2 class="text-2xl md:text-3xl font-black text-white uppercase tracking-tighter mb-2 leading-tight">
         <br> <span class="text-primary">РЕЄСТРАЦІЯ</span>
        </h2>
        <p class="text-stone-500 text-[10px] uppercase tracking-[0.2em] font-bold">Створи свій кастомний профіль</p>
      </header>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "space-y-5" }) do |f| %>
        
        <%# Вивід помилок Devise у нашому стилі %>
        <% if resource.errors.any? %>
          <div class="bg-red-900/20 border-l-4 border-red-500 p-3 rounded-r-lg mb-4">
            <ul class="text-[11px] text-red-400 font-bold uppercase space-y-1">
              <% resource.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="space-y-1.5">
          <%= f.label :email, "Електронна пошта", class: "admin-form-label" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", 
              class: "admin-form-input text-center md:text-left", 
              placeholder: "metalhead@mail.com" %>
        </div>

        <div class="space-y-1.5">
          <div class="flex justify-between items-end">
            <%= f.label :password, "Пароль", class: "admin-form-label !mb-0" %>
            <% if @minimum_password_length %>
              <span class="text-[9px] text-stone-600 font-bold uppercase">Мін. <%= @minimum_password_length %> символів</span>
            <% end %>
          </div>
          <%= f.password_field :password, autocomplete: "new-password", 
              class: "admin-form-input text-center md:text-left", 
              placeholder: "••••••••" %>
        </div>

        <div class="space-y-1.5">
          <%= f.label :password_confirmation, "Підтвердіть пароль", class: "admin-form-label" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", 
              class: "admin-form-input text-center md:text-left", 
              placeholder: "••••••••" %>
        </div>

        <div class="pt-4">
          <%= f.submit "ЗАРЕЄСТРУВАТИСЯ 🤘", class: "btn-primary w-full !py-4 text-base font-black tracking-widest shadow-lg shadow-primary/20 cursor-pointer active:scale-95 transition-transform" %>
        </div>
      <% end %>

      <div class="mt-8 pt-6 border-t border-stone-800 text-center">
        <%= link_to "Вже є акаунт? Увійти", new_session_path(resource_name), 
            class: "text-[11px] font-black uppercase tracking-widest text-stone-500 hover:text-primary transition-colors no-underline" %>
      </div>
    </div>
  </div>
</div>