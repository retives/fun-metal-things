<div class="group bg-stone-900 rounded-2xl border border-stone-800 overflow-hidden hover:border-primary/50 transition-all hover:shadow-2xl hover:shadow-primary/5 flex flex-col">
  <%# Зображення %>
  <div class="relative h-48 bg-stone-950 flex items-center justify-center p-4 overflow-hidden">
    <% if item.image.attached? %>
      <%= image_tag item.image, class: "max-h-full w-auto object-contain group-hover:scale-110 transition-transform duration-500" %>
    <% else %>
      <span class="text-4xl opacity-20">🎸</span>
    <% end %>
    
    <div class="absolute top-3 right-3 bg-stone-900/80 backdrop-blur-sm px-3 py-1 rounded-full border border-stone-700 text-xs text-stone-300 font-mono">
      <%= number_to_currency(item.price, unit: "₴", format: "%n %u") %>
    </div>
  </div>

  <%# Опис %>
  <div class="p-5 flex flex-col flex-1">
    <h3 class="text-white font-bold text-lg mb-2 group-hover:text-primary transition-colors">
      <%= link_to item.name, item %>
    </h3>
    
    <p class="text-stone-500 text-sm line-clamp-2 mb-4 flex-1">
      <%= item.description %>
    </p>

    <div class="flex items-center justify-between mt-auto pt-4 border-t border-stone-800">
       <span class="text-[10px] uppercase tracking-tighter text-stone-600 font-bold">
         <%= item.tags.first&.name || "Instrument" %>
       </span>
       <%= link_to "Детальніше", item, class: "text-xs font-bold text-primary hover:underline" %>
    </div>
  </div>
</div>