<!DOCTYPE html>
<html lang="uk">
  <head>
    <title>FunMetalThings</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="main-header">
      <%= link_to root_path, class: "logo-link" do %>
        <div class="logo-icon">
          <%= image_tag "logo.png", style: "width: 100px; height: auto; display: block;" %>
        </div>
        <span class="logo-text">FunMetalThings</span>
      <% end %>

      <nav class="nav-container">
        <div class="header-item">
        <%= link_to "Каталог", items_path, class: "nav-link" %>
        </div>
        <div class="user-actions">
          <% if user_signed_in? %>
            <%= link_to cart_path, class: "cart-btn" do %>
              <span class="icon">🛒</span>
              <span class="badge"><%= current_cart.cart_items.sum(:quantity) %></span>
              <span class="label">Кошик</span>
            <% end %>

            <%= link_to profile_path, class: "user-profile-link" do %>
              <div class="user-avatar">👤</div>
              <span class="user-email"><%= current_user.email %></span>
            <% end %>

            <%= link_to "Вихід", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn-outline" %>
          <% else %>
            <%= link_to "Вхід", new_user_session_path, class: "nav-link" %>
            <%= link_to "Реєстрація", new_user_registration_path, class: "btn-primary" %>
          <% end %>
        </div>
      </nav>
    </header>

    <main class="content-wrapper">
      <% if notice %> <p class="alert alert-success"><%= notice %></p> <% end %>
      <% if alert %> <p class="alert alert-danger"><%= alert %></p> <% end %>
      
      <%= yield %>
    </main>
  </body>
</html>