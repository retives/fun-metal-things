<div class="content-wrapper" style="max-width: 600px; margin: 50px auto; text-align: center;">
  <div class="item-card" style="padding: 40px; border: 2px solid var(--primary); background: #2b2724;"> 
    <% if @order.present? && @session.present? %>
      <h2 style="color: var(--primary); margin-bottom: 20px;">ПЕРЕХІД ДО ОПЛАТИ 💳</h2>
      
      <div style="background: #332f2c; padding: 20px; border-radius: 8px; margin-bottom: 30px; text-align: left; border-left: 4px solid var(--primary);">
        <p><strong>Замовлення:</strong> №<%= @order.id.to_s.first(8).upcase %></p>
        <p><strong>Сума до сплати:</strong> <span style="color: var(--primary); font-size: 1.2rem; font-weight: bold;">
          <%= number_to_currency(@order.total_price, unit: "₴") %>
        </span></p>
      </div>

      <p style="color: var(--muted); margin-bottom: 25px; font-size: 0.9rem;">
        Натиснувши кнопку нижче, ви будете перенаправлені на захищену сторінку Stripe.
      </p>

      <button id="checkout-button" class="btn-primary" style="width: 100%; padding: 18px; font-size: 1.2rem; letter-spacing: 1px;">
        ОПЛАТИТИ ЧЕРЕЗ STRIPE
      </button>

      <script src="https://js.stripe.com/v3/"></script>
      <script>
        var stripe = Stripe('<%= Rails.application.credentials.dig(:stripe, :publishable_key) || ENV['STRIPE_PUBLISHABLE_KEY'] %>');
        var checkoutButton = document.getElementById('checkout-button');

        checkoutButton.addEventListener('click', function() {
          stripe.redirectToCheckout({
            sessionId: '<%= @session.id %>'
          });
        });
      </script>

    <% else %>
    <div style="padding: 20px; border: 1px dashed red; background: rgba(255,0,0,0.05);">
      <h3 style="color: #ff4500;">Помилка ініціалізації оплати</h3>
      
      <% if @stripe_error %>
        <div style="background: #1a1a1a; color: #ff6b6b; padding: 10px; font-family: monospace; margin: 10px 0; border-radius: 4px;">
          Stripe каже: <%= @stripe_error %>
        </div>
      <% end %>
      <%= link_to "ПОВЕРНУТИСЬ В ПРОФІЛЬ", profile_path, class: "btn-outline" %>
    </div>
  <% end %>

  </div>
</div>
