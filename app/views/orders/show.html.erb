
<% if @order.nil? %>
  <p>Завантаження даних замовлення... Якщо сторінка не оновилась, перейдіть до профілю.</p>
<% else %>
  <div class="item-card">
    <h1 class="page-title">ЧЕК №<%= @order.id.to_s.first(8).upcase %></h1>
  </div>
<% end %>
<div class="content-wrapper content-wrapper--narrow">
  <div class="item-card order-card--highlight">
    
    <%# Штамп статусу %>
    <div class="order-stamp">ОПЛАЧЕНО 🤘</div>

    <h1 class="section-title">ЧЕК №<%= @order.id.to_s.first(8).upcase %></h1>
    <p class="muted">Дякуємо за довіру до FunMetalThings! Твоє замовлення вже готується до відправки.</p>

    <div class="order-items">
      <% @order.order_items.each do |oi| %>
        <div class="order-item-row">
          <span><%= oi.item.name %> (x<%= oi.quantity %>)</span>
          <span class="price" style="font-weight: bold;"><%= number_to_currency(oi.price_at_purchase * oi.quantity, unit: "₴") %></span>
        </div>
      <% end %>
    </div>

    <div style="text-align: right; margin-bottom: 30px;">
      <h2 style="margin: 0;">РАЗОМ: <span class="price"><%= number_to_currency(@order.total_price, unit: "₴") %></span></h2>
    </div>

    <div class="order-info">
      <p><strong>Отримувач:</strong> <%= @order.full_name %></p>
      <p><strong>Адреса:</strong> <%= @order.address %></p>
      <p><strong>Телефон:</strong> <%= @order.phone %></p>
    </div>

    <div class="action-row">
      <%= link_to "НА ГОЛОВНУ", root_path, class: "btn-primary btn-block" %>
      <button onclick="window.print();" class="btn-outline">ДРУКУВАТИ ЧЕК 🖨️</button>
    </div>
  </div>
</div>