<div class="max-w-7xl mx-auto px-4 py-10 md:py-16">
  
  <%# Хедер профілю %>
  <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-10 border-b border-stone-800 pb-6">
    <h1 class="text-3xl md:text-4xl font-black uppercase tracking-tighter">Мій профіль 🎸</h1>
    <%= link_to "Редагувати дані", edit_profile_path, class: "px-6 py-2 border-2 border-primary text-primary hover:bg-primary hover:text-white font-bold rounded-full transition-all text-sm uppercase tracking-widest" %>
  </div>

  <%# Головна сітка: на мобілці колонка, на десктопі 1:2 %>
  <div class="grid grid-cols-1 lg:grid-cols-[1fr_2fr] gap-8 items-start">
    
    <%# ЛІВА КАРТКА: Контакти %>
    <aside class="bg-stone-900 p-6 md:p-8 rounded-2xl border border-stone-800 shadow-xl sticky top-24">
      <h3 class="text-primary font-black uppercase text-lg mb-6 tracking-wide">Контактна інформація</h3>
      
      <div class="space-y-4 text-sm">
        <div class="flex flex-col gap-1">
          <span class="text-stone-500 uppercase text-[10px] font-bold">Email</span>
          <span class="text-white font-medium truncate"><%= current_user.email %></span>
        </div>
        
        <div class="grid grid-cols-2 gap-4">
          <div class="flex flex-col gap-1">
            <span class="text-stone-500 uppercase text-[10px] font-bold">Ім'я</span>
            <span class="text-stone-200"><%= @profile.first_name.presence || "—" %></span>
          </div>
          <div class="flex flex-col gap-1">
            <span class="text-stone-500 uppercase text-[10px] font-bold">Прізвище</span>
            <span class="text-stone-200"><%= @profile.last_name.presence || "—" %></span>
          </div>
        </div>

        <div class="flex flex-col gap-1">
          <span class="text-stone-500 uppercase text-[10px] font-bold">Телефон</span>
          <span class="text-stone-200 font-mono"><%= @profile.phone_number.presence || "—" %></span>
        </div>

        <div class="pt-2">
          <span class="px-3 py-1 bg-stone-800 border border-stone-700 text-stone-400 text-[10px] font-black rounded uppercase tracking-widest">
            <%= current_user.role %>
          </span>
        </div>
      </div>
      
      <div class="mt-8 pt-6 border-t border-stone-800">
        <%= link_to "Змінити пароль", edit_user_registration_path, class: "text-primary/70 hover:text-primary text-xs font-bold transition-colors uppercase" %> 
      </div>
    </aside>

    <%# ПРАВА ЧАСТИНА: Активність %>
    <section class="space-y-12">
      
      <%# Секція замовлень %>
      <div>
        <h3 class="text-xl font-black uppercase mb-6 border-l-4 border-primary pl-4 tracking-tight">Мої замовлення</h3>
        
        <% if @orders.any? %>
          <div class="space-y-4">
            <% @orders.each do |order| %>
              <div class="bg-stone-900/50 p-5 md:p-6 rounded-2xl border border-stone-800 hover:border-stone-700 transition-all flex flex-col sm:flex-row justify-between items-start sm:items-center gap-6">
                
                <div class="space-y-1">
                  <div class="flex items-center gap-3">
                    <h4 class="text-primary font-black text-sm md:text-base">№<%= order.id.to_s.first(8).upcase %></h4>
                    <span class="text-[10px] font-bold px-2 py-0.5 rounded bg-stone-800 text-stone-400 border border-stone-700 uppercase">
                      <%= order.status %>
                    </span>
                  </div>
                  <p class="text-xs text-stone-500">
                    Дата: <%= order.created_at.strftime("%d.%m.%Y") %>
                  </p>
                  <p class="text-lg font-bold text-white mt-2">
                    <%= number_to_currency(order.total_price, unit: "₴") %>
                  </p>
                </div>

                <div class="flex gap-3 w-full sm:w-auto">
                  <%= link_to "Деталі", order_path(order), class: "flex-1 sm:flex-none text-center px-4 py-2 bg-stone-800 hover:bg-stone-700 text-white text-xs font-bold rounded-lg transition-colors" %>
                  <% if order.status == 'pending' %>
                    <%= link_to "ОПЛАТИТИ 💳", payment_order_path(order), 
                        class: "flex-1 sm:flex-none text-center px-4 py-2 bg-primary hover:bg-primary-dark text-white text-xs font-bold rounded-lg shadow-lg shadow-primary/20 transition-all animate-pulse hover:animate-none" %>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="p-10 border-2 border-dashed border-stone-800 rounded-3xl text-center">
            <p class="text-stone-600 text-sm italic">Ви ще не робили замовлень. Пора щось придбати! 🎸</p>
          </div>
        <% end %>
      </div>

      <%# Секція відгуків %>
      <div>
        <h3 class="text-xl font-black uppercase mb-6 border-l-4 border-primary pl-4 tracking-tight">Мої відгуки</h3>
        <% if @reviews.any? %>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <% @reviews.each do |review| %>
              <div class="bg-stone-900 p-5 rounded-2xl border border-stone-800 flex flex-col">
                <div class="flex justify-between items-start mb-4">
                  <strong class="text-white text-sm leading-tight max-w-[70%]"><%= review.item.name %></strong>
                  <span class="text-primary text-xs tracking-tighter"><%= "★" * review.rating %></span>
                </div>
                <p class="text-stone-400 text-xs italic leading-relaxed flex-grow">"<%= review.comment %>"</p>
              </div>
            <% end %>
          </div>
        <% else %>
          <p class="text-stone-600 text-sm italic px-4">Ви ще не залишили жодного відгуку.</p>
        <% end %>
      </div>
    </section>
  </div>
</div>