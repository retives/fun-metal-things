# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>


development:
  adapter: postgresql
  encoding: unicode
  database: fun_metal_things_development
  pool: 5
  username: postgres
  password: 2-Bev45rt # Замініть на пароль, який ви встановлювали для PostgreSQL
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  adapter: postgresql
  encoding: unicode
  database: fun_metal_things_development
  pool: 5
  username: postgres
  password: 2-Bev45rt # Замініть на пароль, який ви встановлювали для PostgreSQL
  host: localhost


# Store production database in the storage/ directory, which by default
# is mounted as a persistent Docker volume in config/deploy.yml.
production:
  adapter: postgresql
  encoding: unicode
  # Render автоматично підставляє DATABASE_URL, тому додаткові поля (user, password) не потрібні
  url: <%= ENV['DATABASE_URL'] %>
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
